import React from 'react'
import Layout from '../components/layout'

const NotFoundPage = () => (
  <Layout>
    <h1>NOT FOUND</h1>
    <p>Here I am, brain the size of a planet, and I can't find this page.</p>
  </Layout>
)

export default NotFoundPage
